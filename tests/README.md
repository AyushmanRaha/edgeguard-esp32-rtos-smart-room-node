# Tests

This folder contains project tests.

- `host_logic_tests/` contains host-side logic tests that can run without ESP32 hardware.
- These tests are used to validate non-hardware project logic safely.
- Hardware and integration tests may be added separately in the future.
